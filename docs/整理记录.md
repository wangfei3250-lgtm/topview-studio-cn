# 整理记录

整理时间：2026-06-21

## 已整理内容

- 补充 `package.json`，加入 `start`、`dev`、`check` 三个脚本
- 补充 `.gitignore`，避免提交运行日志、环境文件和构建目录
- 补充 `README.md`，说明启动方式、文件结构、核心流程和本地接口
- 新增 `docs/功能清单.md`，整理当前页面和模块能力
- 新增 `docs/工作流说明.md`，说明角色、场景、分镜和视频生成链路

## 保留策略

这次没有大拆 `app.js`、`styles.css`、`index.html`，原因是当前功能已经串得比较多：智能体、技能库、任务队列、画布编辑器、工具弹层和短剧流程互相有引用。先补交付结构和说明，避免重构时破坏已有交互。

## 检查结果

已通过：

```bash
npm run check
```

检查内容：

- `app.js` 语法正常
- `server.js` 语法正常

本地服务验证：

```text
http://127.0.0.1:4188/
```

返回状态正常。

## 下一步建议

后续如果要继续工程化，建议按这个顺序拆：

1. 把 `skillCatalog` 拆到 `data/skills.js`
2. 把智能体配置拆到 `data/agents.js`
3. 把工作流逻辑拆到 `modules/workflow.js`
4. 把画布逻辑拆到 `modules/board.js`
5. 把工具弹层逻辑拆到 `modules/tools.js`

这样可以让页面继续保持现在的样子，但代码更好维护。
